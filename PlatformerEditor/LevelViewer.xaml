<UserControl x:Class="PlatformerEditor.LevelViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid x:Name="MainGrid">
        <ScrollViewer PanningMode="Both" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" >
            <Grid x:Name="LevelGrid" MouseDown="LevelGrid_MouseDown" MouseMove="LevelGrid_MouseMove" MouseUp="LevelGrid_MouseUp">
                <Grid x:Name="LevelMap">
                    
                </Grid>
                <Grid x:Name="LevelGridClick" MouseDown="LevelGrid_MouseDown" MouseMove="LevelGrid_MouseMove" MouseUp="LevelGrid_MouseUp" Background="#00000001" />
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
